# backend/Dockerfile
FROM node:18-alpine

WORKDIR /usr/src/app

# ignore dev deps for production image; if you need dev, change accordingly
COPY package*.json ./
RUN npm ci --only=production

COPY . .

ENV NODE_ENV=production
EXPOSE 3000

# If your server entry is server.js (as your screenshot shows)
CMD ["node", "server.js"]
